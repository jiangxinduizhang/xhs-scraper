# 开盘啦首轮画像记录模板

用途：

- 在 Mac mini 上第一次打开 `开盘啦` 时，快速记录可复盘信息
- 不依赖记忆，避免会话结束后丢上下文
- 为后续 `package_name`、`launch_activity`、`host/path`、页面状态机提供事实依据

## 基本信息

- 日期：
- 分支：
- Mac mini 状态：
- 手机型号：
- 手机系统版本：
- root 状态：
- App 版本：
- 备注：

## 环境检查

- `adb devices` 是否正常：
- `adb shell su -c id` 是否返回 root：
- `adb reverse tcp:8080 tcp:8080` 是否成功：
- `mitmdump` 是否正常监听：
- 手机代理是否已写入：
- 手机是否能正常上网：

## App 画像

- 包名：
- 启动 Activity：
- 启动后首屏是什么：
- 首页核心入口有哪些：
- 是否有底部 Tab：
- 是否有搜索入口：
- 是否有登录态 / 未登录态切换：
- 是否有弹窗 / 引导页 / 协议页：

## 页面记录

### 首页

- 页面特征：
- 可见文案：
- 可见按钮：
- 是否能稳定返回：

### 目标页面 1

- 页面名称：
- 进入路径：
- 返回路径：
- 页面特征：
- 可见文案：
- 可见按钮：
- 是否原生页面：
- 是否 WebView：

### 目标页面 2

- 页面名称：
- 进入路径：
- 返回路径：
- 页面特征：
- 可见文案：
- 可见按钮：
- 是否原生页面：
- 是否 WebView：

## 网络观察

- 是否看到 HTTP/HTTPS 请求：
- 是否能解密为 JSON：
- 是否存在 WebSocket：
- 是否存在明显加密 payload：
- 是否有证书校验失败：
- 关键请求路径样例：
- 关键 host 样例：
- 原始响应是否需要留档：

## 初步判断

- 当前最像哪种 App：
- 更像资讯流 / 行情页 / 图表页 / WebView：
- UI 自动化难度：
- 抓包难度：
- 是否值得继续投入：

## 失败点

- 第一个失败点：
- 第二个失败点：
- 最可能的风险：
- 是否需要换策略：

## 下一步动作

- 需要补的包名：
- 需要补的 host/path：
- 需要补的页面状态：
- 需要补的 parser：
- 需要补的动作脚本：


[pytest]
testpaths = tests
markers =
    e2e: marks tests requiring real Android device (deselect with '-m "not e2e"')
    unit: marks tests as unit tests (no device required)
addopts = -v --tb=short -m "not e2e"
